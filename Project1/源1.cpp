#include <iostream>
#include <vector>
#include <string>
#include <list>
#include <array>
#include <iterator>
using namespace std;
int main()
{
	/*int a[][4] = { 1,2,3,6,3,55,65,4,5,4 };
	for (auto &i : a) {
		for (auto &j : i)
			cout << j << " ";
	}
	cout << endl;
	for (auto i = begin(a); i != end(a); ++i) {
		for(auto j=begin(*i);j!=end(*i);++j)
		cout << *j << " ";
	}*/
	//vector <int> ivec(10, 5);
	//for (auto t : ivec)
	//	cout << t << " ";
	//cout << endl;
	//auto temp = ivec.begin() + 3;
	//ivec.insert(temp, 4);
	//for (auto t : ivec)
	//	cout << t << " ";
	//cout << endl;
	////cout << *temp << endl;
	//auto c = ivec.begin();
	//ivec.erase(c);
	//for (auto t : ivec)
	//	cout << t << " ";
	/*string s(10, 's'),s1;
	s.append("hhhh");
	cout << s << endl;
	s1=s.substr(0, 6);
	cout << s1 << endl;
	s.replace(2,2,"jj");
	cout << s << endl;*/

}